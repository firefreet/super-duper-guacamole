<div class="columns is-vcentered">
    <div class="column is-4">
        <div class="buttons is-pulled-right">
            <a data-id="{{id}}" class="button is-primary remake">REMAKE</a>
        </div>
    </div>
    <div class="column is-8">
        <div class="box is-block">
            <article class="media">
                <div class="media-content">
                    <div class="control content has-icons-right is-size-7">
                        <div>{{burger_name}}</div>
                        <i data-id="{{id}}" class="icon is-right fas fa-trash-alt"></i>
                    </div>
                </div>
            </article>
        </div>
    </div>
</div>